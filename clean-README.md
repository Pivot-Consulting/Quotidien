# Quotidien Reset 1.0

Reconstruction minimale et propre : un seul fichier HTML autonome, sans framework, sans build, sans service worker et sans dépendance externe.

Déploiement GitHub Pages : branche `main` ou `gh-pages`, dossier `/ (root)`.
