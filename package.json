{
  "name": "quotidien",
  "version": "3.2.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && node scripts/serve.cjs",
    "lint": "node --check app.js && for file in modules/*.js server/*.cjs; do node --check \"$file\"; done && prettier --check app.js app.css modules src tests scripts server index.html",
    "typecheck": "tsc --noEmit",
    "build": "tsc && node scripts/build.cjs",
    "test": "npm run build && node --test tests/*.test.cjs",
    "verify": "npm run lint && npm run typecheck && npm test",
    "sync:server": "node server/sync.cjs"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "26.1.0",
    "prettier": "3.5.3",
    "typescript": "5.8.3"
  }
}
