{
  "name": "quotidien",
  "version": "2.3.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && node scripts/serve.cjs",
    "lint": "node --check app.js && node --check modules/os-ui.js && node --check modules/personal-ui.js && prettier --check app.js app.css modules src tests scripts index.html",
    "typecheck": "tsc --noEmit",
    "build": "tsc && node scripts/build.cjs",
    "test": "npm run build && node --test tests/*.test.cjs",
    "verify": "npm run lint && npm run typecheck && npm test"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "jsdom": "26.1.0",
    "prettier": "3.5.3"
  }
}
